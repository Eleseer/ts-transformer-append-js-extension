{
	"name": "test-suite",
	"description": "this is a submodule for testing",
	"version": "1.0.0",
	"type": "module",
	"scripts": {
		"prepare": "ts-patch install -s",
		"test": "vitest run"
	},
	"devDependencies": {
		"ts-patch": "^2.1.0",
		"typescript": "^4.9.4",
		"vitest": "^0.26.0"
	}
}
